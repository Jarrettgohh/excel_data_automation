NOTES BELOW IS FOR 1 NEST ARRAY

1. index could be found from number of element in each array
2. columns can be found from the number of array in the array (nested array)

# Example [['a', 'b', 'c'], ['d', 'e', 'f']],
# -> `index` array arg should have 3 elements
# -> `columns` array arg should have 2 elements


